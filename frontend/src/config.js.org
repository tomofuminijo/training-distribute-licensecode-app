export var amplify_config = {
    API: {
        endpoints: [{
            name: 'LicenseCodeDistributorAPI',
            endpoint: 'replace_your_api_url' // API Gateway Endpoint. such as https://abcdefghij.execute-api.us-west-2.amazonaws.com/prod
        }]
    }
};

export default amplify_config;
